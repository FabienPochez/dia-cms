# Dockerfile for jobs service (cron jobs)
# Extends node:18-alpine with bash, rsync, and openssh-client for rsync operations

FROM node:18-alpine

# Install bash, rsync, and openssh-client (required for rsync_pull.sh)
# Add postgresql-client so jobs can update LibreTime DB over TCP (no docker CLI/socket)
RUN apk add --no-cache bash rsync openssh-client postgresql-client

# Set working directory
WORKDIR /app

# Run as node user (uid 1000)
USER node



